# 操作日志（Operation Log）

> 本文件记录知识森林系统的搭建、调整与日常使用动作。**追加记录，不覆盖历史。**

---

## 2026-08-09（搭建日）

### 1. 目录重组（按结构文件）
- 依据「完整文件夹结构.txt」将根目录扁平文件移入分类目录：`文档（docs）`（含 9 个规则文件）、`模板（templates）`、`工具（tools）`、`示例（examples）`、`提示词（prompts）`、`.github/`
- `LICENSE.txt` 重命名为 `LICENSE`
- 「引导问题.txt」内容实为缺失的 `templates/t-leaf.md` 模板，已转存为正式模板后删除原文件

### 2. 文件夹中英双语化
- 格式：**中文在前、英文在后**，如 `文档（docs）`
- 6 个业务目录全部双语化；`.github`（GitHub 特殊目录）与 `.obsidian`（配置目录）保持不变
- 处理坑：`docs` 目录被 Obsidian 锁定无法直接改名，改用「复制到新名 + 删除旧目录」完成，数据无损

### 3. 文件名中英双语化（25 个文件）
- 文档：架构（architecture）、哲学（philosophy）、搭建指南（setup-guide）
- 规则 ×9：土壤与种子、根系系统、养分浓度、主干构建、枝叶、开花、嫁接、修剪与黄叶、结果与反哺
- 模板 ×7：种子模板（t-core-seed）、几何主干模板（t-geometric-trunk）、叶片模板（t-leaf）、问题模板（t-question）、花朵模板（t-flower）、嫁接模板（t-graft）、果实复盘模板（t-fruit-review）
- 工具：伙伴示例（obsidian-buddy-example）.py；示例：设计森林示例（example-design-forest）.md
- 提示词 ×4：生成主干、生成花朵、建议嫁接、复盘果实
- 同步更新全部 17 处文档引用，全局验证无残留

### 4. 安装 Obsidian 插件
- **Templater v2.25.0**：下载 release → 解压至 `.obsidian/plugins/templater-obsidian/` → 注册
- **Dataview v0.5.70**：下载 main.js/manifest.json/styles.css → `.obsidian/plugins/dataview/` → 注册
- `community-plugins.json` 现为 `["templater-obsidian", "dataview"]`
- 网络说明：GitHub 直连超时，经 gh-proxy.com 镜像下载

### 5. 创建使用区文件夹（7 个）
- 按使用指南创建：土壤（soil）、花朵（Flowers）、果实（Fruits）、堆肥（Compost）、档案（Archive）、系统（System）、使用模板（templates）
- 后经调整，使用区文件夹统一放入 `森林知识库/` 子目录，与系统文件夹（文档/模板/工具/提示词/示例）分离；模板源保留在根目录 `模板（templates）/`

### 6. 文档引用同步更新
- 规则文档 + README + 搭建指南 + 架构 + 示例：`soil/` → `土壤（soil）` 等全部文件夹引用改为双语名
- 新增用户文档「知识森林系统 v2.3 — 完整使用指南.md」一并同步（约 20 处引用）
- 整合文档 `E:\知识森林系统.md` 同步：25 个章节标题 + 完整结构树 + 全部内嵌引用，与磁盘结构完全一致

### 7. 规则引用影响评估
- 结论：文件夹移入 `森林知识库/` 子目录后，双链、标签、Dataview 全库查询、模板引用均不受影响（Obsidian 按文件名/标签全局解析）
- 仅文件夹文字描述需同步（已在上条完成）

### 8. 知识树生长：读书领域
- **扫描收件箱** → 发现碎片《第一性原理》(149 页 PDF) → 分析内容 → 用户确认后执行：
  - 建叶片 `森林知识库/读书/第一性原理-读书笔记.md`（9 标签）
  - 生成几何主干 `森林知识库/读书/树-思维模型.md`（核心概念/层级/因果链/张力/Mermaid 图）
  - 补全种子 `读书.md`（domain=教育/成长/学习，status=sprouting，年轮日志）
- **第二片叶子**《动机心理学》(103 页 PDF) → 用户确认后执行：
  - 建叶片 `森林知识库/读书/动机心理学-读书笔记.md`（9 标签）
  - 种子根链接 + 年轮日志更新
  - 主干挂新枝「行为动机」（核心概念 + Mermaid 图新增节点）
- 双链网络已闭合：读书 ↔ 第一性原理-读书笔记 ↔ 树-思维模型 ↔ 动机心理学-读书笔记
- 约定：`文档（docs）/哲学（philosophy）.md` 为系统核心思想/底层逻辑，**不建立任何笔记链接**

### 9. 工具环境
- 隔离 Python 环境已具备 pypdf + markitdown（PDF/Office 文档解析）

---

## 2026-08-10

### 1. 使用指南同步更新
- 用户删除「系统（System）」文件夹（原定义存放 operation-log），操作日志改为记录在**根目录 `log.md`**
- 同步更新「知识森林系统 v2.3 — 完整使用指南.md」：
  - 文件夹定义表：7 行改为 5 个使用区文件夹，注明统一位于 `森林知识库/` 子目录
  - 新增说明：操作日志 → 根目录 `log.md`；模板 → 直接指向根目录 `模板（templates）`
  - 系统文件位置说明同步（模板/文档/提示词/工具/示例在根目录，使用区在森林知识库）
- 全局验证无 System/operation-log 残留

---

_下次动作请继续在本文件追加记录。_
